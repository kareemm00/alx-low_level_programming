#include <stdio.h>

/**
 * main - start of the program
 *
 * Description: a c program that prints numbers from 0 to 9
 *
 * Return: 0 (success)
*/

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
