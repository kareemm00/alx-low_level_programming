#include <stdio.h>

/**
 * main - start of the program
 *
 * Description: a c program that prints numbers from 0 to 9
 *
 * Return: 0 (success)
*/

int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
