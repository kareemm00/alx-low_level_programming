#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 *
 * @s: parameter string
 *
 * Return: length of a string
*/

int _strlen(char *s)
{
	int length;

	for (length = 0; *s != '\0'; s++)
		lenght++;
	return (length);
}
